# 加密狗（SafeNet / SuperDog）编译宏配置说明

> 适用项目：winform
> 相关修改：调整加密狗：debug 默认不启用加密狗，打包使用 release 默认启用加密狗

---

## 1. 背景与目的

软件通过加密狗（SafeNet / SuperDog）做授权校验：没有插狗时程序应拒绝运行。

过去在两处源码里用 **`#define useSafeDog` + `#if useSafeDog ... #endif`** 的方式开启校验：
- `Main/Program.cs` —— 程序启动前校验
- `Main/Forms/MainForm.cs` —— 运行期间定时监控狗是否被拔出

但 `#define` 是**文件级作用域**，带来两个麻烦：

1. **本地调试被卡**：自己电脑上没插狗时，直接按 Debug 运行会被拦在验狗逻辑外，每次得先手动注释掉这两处的宏，调试完再恢复。
2. **容易误提交**：为了本地能跑而注释掉的代码，一不小心就跟着 commit 推上去了，别人拉下来或打包时就少了加密狗保护。

**目标**：让宏定义由构建配置（Debug / Release）统一控制 ——
- **Debug**：默认**不启用**加密狗，本机想怎么调怎么调；
- **Release**：默认**启用**加密狗，打包发给客户的程序必须插狗才能用。

---

## 2. 改动内容

### 2.1 删除源码里的 `#define`

| 文件 | 位置 | 改动 |
|------|------|------|
| `Main/Program.cs` | 第 1 行 | 删除 `#define useSafeDog`，保留 `using` |
| `Main/Forms/MainForm.cs` | 第 1 行 | 删除 `#define useSafeDog`，保留 `using` |

两处源码**保留**原有的 `#if useSafeDog ... #endif` 包裹块，只是不再各自定义宏。

### 2.2 把宏定义收归 `Main/Main.csproj`

在 `Main.csproj` 的 Release 配置中加入 `useSafeDog` 宏（与原有 `TRACE` 并列）：

```xml
<!-- Debug 配置（第 37 行）：不含 useSafeDog，本地免狗调试 -->
<PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
  <DefineConstants>DEBUG;TRACE</DefineConstants>
  ...
</PropertyGroup>

<!-- Release 配置（第 47 行）：含 useSafeDog，打包必验狗 -->
<PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
  <DefineConstants>TRACE;useSafeDog</DefineConstants>
  ...
</PropertyGroup>
```

现在 `useSafeDog` 是**整个程序集级别**生效，两处 `#if` 永远同步，彻底告别"漏改一个文件导致半吊子校验"。

---

## 3. 编译行为对照

| 构建配置 | `DefineConstants` | `useSafeDog` | 启动前验狗 | 运行期监控狗拔出 |
|----------|-------------------|--------------|------------|------------------|
| Debug    | `DEBUG;TRACE`     | 未定义       | 跳过       | 不启动           |
| Release  | `TRACE;useSafeDog`| 已定义       | 执行       | 启动（每秒检测） |

对应代码片段（保持不变，仅受宏控制）：

```csharp
// Main/Program.cs  (第 26–33 行)
#if useSafeDog
    if ((!DogHelper.LoginSafeNetDog()) && (!DogHelper_Max.LoginSafeNetDog()))
    {
        MessageBox.Show(UILocalize.NotFoundDog, ...);
        return;
    }
#endif

// Main/Forms/MainForm.cs  (第 202–204 行)
_dogCheckTimer.Tick += (o, ea) =>
{
#if useSafeDog  //不使用加密狗测试
    SuperDogMonitor();
#endif
};
```

---

## 4. 验证方法

1. **Debug 免狗**：VS 选 Debug | AnyCPU，直接 F5，不插狗也能进主界面；IDE 中 `#if useSafeDog` 块会变灰（正常，因为当前配置未定义该宏）。
2. **Release 验狗**：VS 选 Release | AnyCPU 重新生成；把 `bin/` 下产物拷到任意一台未插狗的机器，启动应弹"未找到加密狗"并退出。
3. **提交安全**：`git status` 干净，无需为了本地调试而注释任何代码。

---

## 5. 注意事项

- **新增平台配置要补宏**：`Main.csproj` 目前只有 `Debug|AnyCPU` 与 `Release|AnyCPU` 两组。若后续新增如 `Release|x64`，需在其 `DefineConstants` 里也带上 `useSafeDog`，否则该配置打出的包会悄悄失去加密狗保护。
  建议加一段与平台无关的兜底，直接放在原release之后，确保任意平台的 Release 都带宏：
  
  ```xml
  <PropertyGroup Condition=" '$(Configuration)' == 'Release' AND !$(DefineConstants.Contains('useSafeDog')) ">
    <DefineConstants>$(DefineConstants);useSafeDog</DefineConstants>
  </PropertyGroup>
  ```
  
  
